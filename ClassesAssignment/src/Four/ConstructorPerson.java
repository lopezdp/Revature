package Four;

public class ConstructorPerson {
	
	/*
	 * 
	 * Write a class named ConstructorPerson 
	 * that declares name (String) and age (int) 
	 * instance variables. 
	 * 
	 * Write a no-arg constructor1 that initializes 
	 * ConstructorPerson with default values. 
	 * 
	 * In a Main class, create an instance of a 
	 * ConstructorPerson and print its properties name and age.  
	 * 
	 * Place these files in a package named, “four”. 
	 * 
	 */
	String name;
	int age;
	
	ConstructorPerson() {
		this.name = "Dave";
		this.age = 19;
	}

}
